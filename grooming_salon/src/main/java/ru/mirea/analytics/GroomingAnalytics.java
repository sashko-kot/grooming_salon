// Указываем пакет, к которому принадлежит класс.
// Пакет — это "папка" для классов, которая помогает организовать код по смыслу
// (analytics — сюда положена логика анализа данных, а не хранение или веб-контроллеры).
package ru.mirea.analytics;

// Импортируем класс GroomingService из пакета ru.mirea.model.
// import нужен, потому что GroomingService объявлен в ДРУГОМ пакете (ru.mirea.model),
// а не в текущем (ru.mirea.analytics). Без импорта компилятор не поймёт, что за тип GroomingService.
import ru.mirea.model.GroomingService;

// BigDecimal — специальный класс Java для ТОЧНЫХ денежных и дробных вычислений.
// Обычный double/float хранит дробные числа неточно (из-за особенностей двоичного
// представления), поэтому для денег (цена услуги) используют BigDecimal — он не теряет точность.
import java.math.BigDecimal;

// RoundingMode — перечисление (enum) со способами округления чисел.
// Используется вместе с BigDecimal, когда нужно явно сказать, КАК округлять
// (например, HALF_UP — "округление вверх, если дробная часть >= 0.5").
import java.math.RoundingMode;

// Comparator — интерфейс для описания правила сравнения двух объектов.
// Нужен, чтобы сказать stream()'у, ПО КАКОМУ полю сортировать или искать максимум/минимум
// (например, "сравнивать по цене" или "сравнивать по длительности").
import java.util.Comparator;

// List — интерфейс для представления списка (упорядоченной коллекции элементов).
// Используется практически во всех методах класса, так как метод принимает
// "список услуг" и должен вернуть "список услуг после фильтрации/сортировки".
import java.util.List;

// Map — интерфейс "словаря": хранит пары ключ-значение.
// Здесь используется в методе groupByDuration, где ключ — это название категории
// ("Короткие" / "Долгие"), а значение — список услуг этой категории.
import java.util.Map;

// Collectors — вспомогательный класс с готовыми "сборщиками" для Stream API.
// Используется, чтобы превратить поток (Stream) элементов в конкретную структуру
// данных — например, Collectors.groupingBy(...) группирует элементы потока в Map.
import java.util.stream.Collectors;

// Комментарий-описание класса: кратко объясняет, ЗАЧЕМ нужен этот класс.
// Класс для анализа списка услуг: поиск, расчёты и группировка.

// "public" — модификатор доступа. Означает, что класс виден и может использоваться
// из ЛЮБОГО другого пакета проекта (не только из ru.mirea.analytics).
// Если бы модификатора не было (package-private), класс был бы виден только
// классам внутри пакета ru.mirea.analytics — а сервисам/контроллерам из других
// пакетов (например, ru.mirea.web) пришлось бы делать GroomingAnalytics видимым снаружи,
// иначе они не смогли бы его использовать. Так как аналитика обычно вызывается
// откуда-то извне (например, из сервисного слоя или контроллера), класс сделан public.
//
// "class" (а не "record") — потому что этот тип НЕ хранит просто набор неизменяемых
// данных (как GroomingService — вероятно, record с полями title/price/duration).
// GroomingAnalytics — это класс-"сервис" без собственных полей с данными:
// он не описывает "сущность" (данные), а описывает ПОВЕДЕНИЕ (методы-операции
// над чужими данными: найти, посчитать среднее, сгруппировать). Record же создаётся
// именно для неизменяемых данных с автоматически сгенерированными конструктором,
// геттерами, equals/hashCode/toString — здесь это не нужно, поэтому используется
// обычный class.
public class GroomingAnalytics {

    // === Метод 1: поиск услуг дешевле заданной цены ===

    // "public" — метод должен вызываться извне класса (например, из сервисного слоя
    // или REST-контроллера), поэтому он открыт для использования другими классами.
    // Возвращаемый тип "List<GroomingService>" — метод отдаёт список подходящих
    // услуг.
    // Параметры:
    // services — список ВСЕХ услуг, среди которых нужно искать;
    // maxPrice — верхняя граница цены (BigDecimal — та же логика точности, что и
    // выше).
    public List<GroomingService> findServicesCheaperThan(List<GroomingService> services, BigDecimal maxPrice) {

        // Проверка на null — защита от NullPointerException в дальнейшем коде.
        // Если вызывающий код передал maxPrice == null, дальнейшее сравнение цены
        // (s.price().compareTo(maxPrice)) упадёт с ошибкой. Поэтому мы сразу
        // прерываем выполнение метода понятным сообщением об ошибке.
        if (maxPrice == null) {
            // IllegalArgumentException — стандартное unchecked-исключение Java,
            // которое принято бросать, когда в метод передан некорректный аргумент.
            // "throw" — ключевое слово, которое прерывает выполнение метода
            // и передаёт объект исключения вызывающему коду.
            throw new IllegalArgumentException("Максимальная цена не может быть пустой.");
        }

        // services.stream() — превращаем список в "поток" (Stream), чтобы можно было
        // последовательно применить к нему операции фильтрации/сортировки в
        // функциональном стиле
        // (вместо классического цикла for с ручным добавлением в новый список).
        return services.stream()
                // .filter(...) — оставляет в потоке только те элементы, для которых
                // переданное лямбда-выражение (условие) вернёт true.
                // "s -> ..." — это лямбда-выражение: "s" — параметр (одна услуга из потока),
                // "->" отделяет параметр от тела функции, справа — само условие.
                //
                // s.price() != null — проверяем, что у услуги вообще указана цена
                // (если это record, price() — это автоматически сгенерированный геттер поля
                // price).
                // s.price().compareTo(maxPrice) <= 0 — сравниваем BigDecimal-значения.
                // У BigDecimal нельзя использовать "<=" напрямую (это не примитивный тип),
                // поэтому используется метод compareTo(), который возвращает:
                // отрицательное число, если "price" МЕНЬШЕ "maxPrice",
                // 0, если они равны,
                // положительное число, если "price" БОЛЬШЕ "maxPrice".
                // Условие "<= 0" означает "цена меньше или равна максимальной".
                .filter(s -> s.price() != null && s.price().compareTo(maxPrice) <= 0)
                // .sorted(...) — сортирует элементы потока согласно переданному Comparator.
                // Comparator.comparing(GroomingService::price) — создаёт правило сравнения
                // "по возрастанию цены". GroomingService::price — это ссылка на метод
                // (method reference), сокращённая запись лямбды "s -> s.price()".
                .sorted(Comparator.comparing(GroomingService::price))
                // .toList() — завершающая операция потока: собирает все оставшиеся
                // после filter/sorted элементы в обычный неизменяемый List<GroomingService>.
                .toList();
    }

    // === Метод 2: расчёт средней цены ===

    // Возвращает BigDecimal — среднюю цену по переданному списку услуг.
    public BigDecimal calculateAveragePrice(List<GroomingService> services) {

        // Проверяем сразу ДВА "пустых" случая одним условием:
        // services == null — список вообще не передали (ссылка пустая);
        // services.isEmpty() — список передали, но в нём 0 элементов.
        // В обоих случаях считать среднее не из чего, поэтому возвращаем
        // BigDecimal.ZERO —
        // готовую константу класса BigDecimal, представляющую число 0.
        if (services == null || services.isEmpty()) {
            return BigDecimal.ZERO;
        }

        // Переменная "avg" объявлена как примитивный тип "double" (а не BigDecimal),
        // потому что Stream API для чисел с плавающей точкой удобнее работает
        // именно через DoubleStream (см. .mapToDouble ниже), а финальное округление
        // мы всё равно сделаем через BigDecimal чуть позже — так и точность денег
        // сохраняется в итоговом результате, и промежуточные вычисления делать проще.
        double avg = services.stream()
                // Отфильтровываем услуги без цены — иначе следующий шаг (mapToDouble) упадёт
                // с NullPointerException при попытке вызвать .doubleValue() у null.
                .filter(s -> s.price() != null)
                // .mapToDouble(...) — специальная версия map(), которая ПРЕВРАЩАЕТ
                // Stream<GroomingService> в DoubleStream (поток примитивных double).
                // Это нужно, чтобы дальше можно было вызвать .average() — такого метода
                // нет у обычного Stream<T>, он есть только у специализированных
                // потоков чисел (IntStream, DoubleStream, LongStream).
                // s.price().doubleValue() — конвертируем BigDecimal в обычное double.
                .mapToDouble(s -> s.price().doubleValue())
                // .average() — считает среднее арифметическое всех чисел в потоке.
                // Возвращает НЕ double напрямую, а объект OptionalDouble — специальную
                // "обёртку", которая может либо содержать число, либо быть пустой
                // (пустой она будет, если поток вообще не содержал элементов).
                .average()
                // .orElse(0.0) — если OptionalDouble пуст (нет элементов после filter),
                // используем 0.0 как запасное значение вместо ошибки.
                .orElse(0.0);

        // BigDecimal.valueOf(avg) — создаёт объект BigDecimal из примитивного double.
        // .setScale(2, RoundingMode.HALF_UP) — обрезает/округляет число до 2 знаков
        // после запятой (это стандарт для денег — рубли и копейки).
        // RoundingMode.HALF_UP — правило округления "0.5 и больше — округляем вверх"
        // (например, 10.235 округлится до 10.24).
        return BigDecimal.valueOf(avg).setScale(2, RoundingMode.HALF_UP);
    }

    // Метод 3: расчёт медианы

    // половина цен меньше медианы, половина — больше (в отличие от среднего,
    // медиана устойчива к "выбросам" — например, к одной очень дорогой услуге).
    public BigDecimal calculateMedianPrice(List<GroomingService> services) {

        // Та же защита от null/пустого списка, что и в методе выше.
        if (services == null || services.isEmpty()) {
            return BigDecimal.ZERO;
        }

        // Собираем ОТДЕЛЬНЫЙ список цен (без остальных полей услуги),
        // потому что для расчёта медианы нужны только числа, отсортированные по
        // возрастанию.
        List<BigDecimal> prices = services.stream()
                // .map(GroomingService::price) — для каждой услуги "превращаем" её
                // в её же цену (GroomingService::price — ссылка на метод-геттер).
                // После этой строки поток перестаёт быть потоком GroomingService
                // и становится потоком BigDecimal (Stream<BigDecimal>).
                .map(GroomingService::price)
                // Убираем услуги без цены (price == null), чтобы не мешали сортировке.
                .filter(price -> price != null)
                // .sorted() без аргументов — сортирует BigDecimal "по естественному порядку"
                // (BigDecimal реализует интерфейс Comparable, поэтому Java знает, как
                // сравнивать два BigDecimal, даже без явного Comparator).
                .sorted()
                .toList();

        // Дополнительная проверка: если ПОСЛЕ фильтрации по null список стал пустым
        // (например, у всех услуг цена была null), медиану считать не из чего.
        if (prices.isEmpty()) {
            return BigDecimal.ZERO;
        }

        // Вычисляем индекс "середины" списка. Целочисленное деление (int / int)
        // в Java всегда отбрасывает дробную часть, поэтому middle — это индекс
        // элемента чуть правее середины (для чётного количества элементов).
        int middle = prices.size() / 2;

        // Проверяем чётность количества элементов через остаток от деления на 2.
        if (prices.size() % 2 == 0) {
            // Если элементов чётное количество (например, 4: индексы 0,1,2,3),
            // "настоящей середины" нет — берём ДВА центральных элемента
            // (middle - 1 и middle, для 4 элементов это индексы 1 и 2)
            // и находим их среднее арифметическое.
            BigDecimal left = prices.get(middle - 1);
            BigDecimal right = prices.get(middle);

            // .add(...) — складывает два BigDecimal (аналог "+" для чисел).
            // .divide(BigDecimal.valueOf(2), 2, RoundingMode.HALF_UP) — делит сумму на 2,
            // сразу указывая точность результата (2 знака после запятой) и способ
            // округления.
            // Явно указывать точность и округление ОБЯЗАТЕЛЬНО для BigDecimal.divide(),
            // иначе при делении, которое даёт бесконечную дробь, метод бросит исключение
            // ArithmeticException.
            return left.add(right).divide(BigDecimal.valueOf(2), 2, RoundingMode.HALF_UP);
        }

        // Если элементов нечётное количество (например, 5: индексы 0,1,2,3,4),
        // "middle" (индекс 2) — это и есть точная середина списка.
        // Просто приводим найденное число к 2 знакам после запятой для единообразия
        // формата.
        return prices.get(middle).setScale(2, RoundingMode.HALF_UP);
    }

    // === Метод 4: группировка услуг по длительности ===

    // Возвращаемый тип Map<String, List<GroomingService>> — "словарь", где
    // ключ (String) — название категории, значение (List<GroomingService>) —
    // список услуг, попавших в эту категорию.
    public Map<String, List<GroomingService>> groupByDuration(List<GroomingService> services) {
        return services.stream()
                // Collectors.groupingBy(...) — собирает элементы потока в Map,
                // группируя их по результату переданной функции ("классификатора").
                // Для каждой услуги вычисляется "ключ" (строка-категория),
                // и услуга добавляется в список внутри Map под этим ключом.
                .collect(Collectors.groupingBy(
                        // Тернарный оператор "условие ? значение_если_true : значение_если_false" —
                        // компактная замена if/else в одно выражение.
                        // s.durationMinutes() < 60 — если услуга короче часа,
                        // ключом станет строка "Короткие (< 60 мин)",
                        // иначе — "Долгие (>= 60 мин)".
                        s -> s.durationMinutes() < 60 ? "Короткие (< 60 мин)" : "Долгие (>= 60 мин)"));
    }

    // === Метод 5: поиск услуг по ключевому слову ===

    public List<GroomingService> searchByKeyword(List<GroomingService> services, String keyword) {

        // keyword == null — слово для поиска вообще не передали;
        // keyword.isBlank() — передали, но это пустая строка или строка из одних
        // пробелов
        // (isBlank() — метод String, появившийся в Java 11, проверяет именно это, в
        // отличие
        // от isEmpty(), который считает строку " " НЕ пустой).
        if (keyword == null || keyword.isBlank()) {
            // List.of() — создаёт пустой НЕИЗМЕНЯЕМЫЙ список (попытка добавить в него
            // элемент вызовет исключение). Возвращаем его вместо null, чтобы вызывающий
            // код мог сразу писать цикл по результату, не проверяя его на null.
            return List.of();
        }

        // .trim() — убирает пробелы по краям строки (" кот " -> "кот").
        // .toLowerCase() — приводит строку к нижнему регистру, чтобы поиск
        // не зависел от регистра букв ("Кот" и "кот" считались одинаковыми).
        String normalizedKeyword = keyword.trim().toLowerCase();

        return services.stream()
                // Условие фильтрации: услуга подходит, если ключевое слово встречается
                // ЛИБО в названии (title), ЛИБО в описании (description).
                //
                // s.title() != null && s.title().toLowerCase().contains(normalizedKeyword)
                // — сначала проверяем title на null (иначе toLowerCase() упадёт с NPE),
                // затем приводим его к нижнему регистру и проверяем, содержит ли
                // он подстроку normalizedKeyword методом .contains().
                //
                // Знак "||" — логическое ИЛИ: условие в целом истинно, если истинно
                // ХОТЯ БЫ ОДНО из двух условий (совпадение в title ИЛИ в description).
                //
                // Важный нюанс приоритета операторов в этой строке:
                // "&&" имеет более высокий приоритет, чем "||", поэтому условие
                // фактически читается как:
                // (title != null && title.contains(...)) || (description != null &&
                // description.contains(...))
                // то есть проверка на null для description относится ТОЛЬКО к правой части,
                // а не ко всему выражению целиком — это важно понимать при чтении кода.
                .filter(s -> s.title() != null && s.title().toLowerCase().contains(normalizedKeyword)
                        || s.description() != null && s.description().toLowerCase().contains(normalizedKeyword))
                .toList();
    }

    // === Метод 6: поиск самой долгой услуги ===

    // Возвращает GroomingService (один объект, а не список) — саму "долгую" услугу,
    // либо null, если подходящей услуги нет.
    public GroomingService getLongestService(List<GroomingService> services) {

        if (services == null || services.isEmpty()) {
            // Явно возвращаем null, если анализировать нечего.
            // (Альтернативой было бы вернуть Optional<GroomingService>, что считается
            // более безопасным стилем в современной Java, но в этом классе выбран
            // более простой вариант — вызывающий код должен сам проверять на null.)
            return null;
        }

        return services.stream()
                // .max(...) — находит МАКСИМАЛЬНЫЙ элемент потока по переданному Comparator.
                // Comparator.comparingInt(GroomingService::durationMinutes) — правило
                // "сравнивать услуги по их длительности в минутах (примитивное int-поле)".
                // comparingInt используется вместо обычного comparing(), потому что
                // durationMinutes(), скорее всего, возвращает примитивный int,
                // а comparingInt работает с int без лишней "упаковки" в Integer.
                .max(Comparator.comparingInt(GroomingService::durationMinutes))
                // .max() возвращает Optional<GroomingService> (может быть пустым,
                // если поток пуст). .orElse(null) — если Optional пуст, вернуть null.
                .orElse(null);
    }

    // === Метод 7: вывод услуг по категориям в консоль ===

    // Возвращаемый тип "void" — метод ничего не возвращает, а только производит
    // побочный эффект (печать в консоль). Такие методы обычно называют командами,
    // в отличие от методов-запросов (find*, calculate*), которые возвращают
    // результат.
    public void printServicesByDurationCategory(List<GroomingService> services) {
        System.out.println("--- Группировка по длительности ---");

        // Переиспользуем уже написанный метод groupByDuration вместо дублирования
        // логики группировки — хороший пример принципа "не повторяйся" (DRY).
        //
        // .forEach((category, list) -> {...}) — метод Map для перебора ВСЕХ пар
        // ключ-значение. В лямбде два параметра: "category" — ключ (String),
        // "list" — значение (List<GroomingService>) для этого ключа.
        groupByDuration(services).forEach((category, list) -> {
            System.out.println(category + ":");

            // Для каждой услуги из списка конкретной категории печатаем строку вида
            // " - Стрижка (45 мин)". Два пробела в начале строки — просто отступ
            // для визуальной вложенности в консоли (категория, а под ней — её услуги).
            list.forEach(s -> System.out.println("  - " + s.title() + " (" + s.durationMinutes() + " мин)"));
        });
    }
}
// Пакет "repository" — здесь находится код доступа к данным: реализация того,
// КАК конкретно сущность GroomingService читается/сохраняется в хранилище
// (в данном случае — в реляционной базе данных через JDBC).
package ru.mirea.repository;

// Connection — интерфейс, представляющий одно открытое соединение с базой данных.
import java.sql.Connection;

// PreparedStatement — "подготовленный" SQL-запрос с параметрами-заполнителями "?".
// В отличие от обычного Statement (использованного в DatabaseConnection для
// фиксированных DDL-запросов CREATE TABLE), здесь PreparedStatement применяется
// ВЕЗДЕ, потому что каждый метод класса подставляет в SQL значения, которые
// приходят "снаружи" (например, из формы ввода пользователя или от вызывающего
// кода) — id, title, price и т.д. Подстановка через "?" и последующие
// ps.setXxx(...) защищает от SQL-инъекций (внешние данные никогда не попадают
// в текст SQL напрямую как строка, а передаются драйверу БД отдельно,
// как параметры, и не могут быть интерпретированы как часть SQL-команды).
import java.sql.PreparedStatement;

// ResultSet — результат SQL-запроса SELECT: "курсор", по которому можно построчно
// пройти и прочитать полученные из базы данных значения.
import java.sql.ResultSet;

// SQLException — checked-исключение JDBC, которое обязательно нужно либо
// обработать (try-catch), либо объявить через "throws" у метода. В этом классе
// оно везде ПОЙМАНО (catch) и обёрнуто в RuntimeException — подробнее см. ниже
// в комментариях к конкретным методам, почему выбран именно такой подход.
import java.sql.SQLException;

// Statement — используется здесь не для выполнения запроса напрямую, а только
// ради одной его константы: Statement.RETURN_GENERATED_KEYS (см. метод save()).
import java.sql.Statement;

// ArrayList — конкретная (можно создавать через "new") реализация интерфейса List,
// основанная на массиве, который умеет сам увеличиваться при необходимости.
// Используется в методе findAll() для накопления найденных строк результата.
import java.util.ArrayList;

// List — интерфейс списка, используется как ТИП переменных/возвращаемых значений
// (в коде почти всегда предпочитают ссылаться на интерфейс List, а не на
// конкретную реализацию ArrayList, — это позволяет в будущем безболезненно
// заменить реализацию списка, не переписывая сигнатуры методов).
import java.util.List;

// Optional<T> — специальная "обёртка" вокруг значения, которое МОЖЕТ отсутствовать.
// Используется в методе findById(), чтобы явно показать в сигнатуре метода:
// "услуга с таким id может быть не найдена, и это нормальная, ожидаемая ситуация,
// а не ошибка" — в отличие от возврата null, где вызывающий код мог бы забыть
// проверить результат на null и получить NullPointerException.
import java.util.Optional;

// Импортируем модель GroomingService из пакета ru.mirea.model — этот репозиторий
// работает именно с объектами такого типа: читает их из базы и записывает туда же.
import ru.mirea.model.GroomingService;

// Комментарий-описание класса.
// Реализация репозитория через JDBC.
// С помощью SQL-запросов она работает напрямую с таблицей услуг в базе данных.

// "public" — класс используется извне пакета repository (например, сервисным
// слоем или контроллером, которому нужно получить/сохранить услуги).
//
// "class" (БЕЗ "final" в этот раз, в отличие от DatabaseConnection и GroomingExporter) —
// этот класс НЕ помечен как final, и это осмысленно: в отличие от чисто
// утилитных классов со статическими методами, GroomingServiceJdbcRepository —
// это ОБЫЧНЫЙ объект с методами экземпляра (не static), который явно реализует
// интерфейс (см. ниже). Такой класс вполне может быть, при необходимости,
// расширен (например, для добавления кэширования поверх JDBC-логики через
// наследование), поэтому финализировать его не стали — хотя на практике
// в современном Java-коде для таких случаев чаще предпочитают композицию,
// а не наследование, отсутствие "final" здесь просто оставляет техническую
// возможность для наследования, если она понадобится.
//
// "implements GroomingServiceRepository" — класс реализует ИНТЕРФЕЙС
// GroomingServiceRepository (его самого не видно в этом файле — он объявлен
// отдельно), то есть берёт на себя обязательство реализовать ВСЕ методы,
// перечисленные в этом интерфейсе (save, findById, findAll, update, deleteById —
// судя по методам ниже, помеченным аннотацией @Override).
// Смысл такого разделения на интерфейс и реализацию: остальной код проекта
// (сервисы, контроллеры) может зависеть от АБСТРАКЦИИ GroomingServiceRepository,
// не зная, что конкретно "под капотом" — JDBC, JPA/Hibernate, обычный список
// в памяти для тестов и т.д. Это позволяет, например, подменить JDBC-реализацию
// на другую (скажем, GroomingServiceInMemoryRepository для юнит-тестов)
// без изменения кода, который использует репозиторий, — классический принцип
// "программируй на уровне интерфейса, а не реализации".
public class GroomingServiceJdbcRepository implements GroomingServiceRepository {

    // Комментарий-описание метода.
    // Сохраняет новую услугу в БД.

    // "@Override" — аннотация, которая говорит компилятору: "этот метод обязан
    // переопределять метод, объявленный в родительском классе или интерфейсе".
    // Она не влияет на работу программы напрямую, но служит СТРАХОВКОЙ:
    // если бы в интерфейсе GroomingServiceRepository метод назывался, например,
    // "saveService", а не "save", или принимал другие параметры, компилятор
    // выдал бы ОШИБКУ прямо на этой аннотации ("метод не переопределяет метод
    // из надкласса"), а не позволил бы тихо создать новый, никак не связанный
    // с интерфейсом метод с похожим именем.
    // "public" — реализация метода интерфейса ОБЯЗАНА быть public, так как
    // сам метод в интерфейсе объявлен неявно как public (все методы интерфейсов
    // по умолчанию public), и в Java нельзя "сузить" видимость при переопределении.
    // "void" — метод ничего не возвращает, просто выполняет сохранение.
    @Override
    public void save(GroomingService service) {
        // SQL-запрос на добавление новой строки. Символы "?" — это ПОЗИЦИОННЫЕ
        // параметры-заполнители, значения для которых подставляются НИЖЕ через
        // ps.setXxx(номер_параметра, значение). Заметьте: колонка "id" здесь
        // НЕ указана — она заполнится автоматически самой базой данных
        // благодаря типу BIGSERIAL (см. комментарии к DatabaseConnection),
        // поэтому в INSERT перечислены только 4 колонки и 4 знака "?".
        String sql = "INSERT INTO grooming_services (title, duration_minutes, price, description) VALUES (?, ?, ?, ?)";

        // try-with-resources: Connection и PreparedStatement реализуют
        // AutoCloseable, поэтому оба будут автоматически закрыты по выходу
        // из блока try — даже если внутри произойдёт исключение.
        //
        // DatabaseConnection.getConnection() — обращение к статическому методу
        // ИЗ ДРУГОГО КЛАССА того же пакета repository (см. отдельно прокомментированный
        // файл DatabaseConnection) — этот класс не импортируется отдельной строкой
        // import, так как он находится В ТОМ ЖЕ пакете ru.mirea.repository,
        // а классы одного пакета видят друг друга без import.
        //
        // conn.prepareStatement(sql, Statement.RETURN_GENERATED_KEYS) — второй
        // аргумент Statement.RETURN_GENERATED_KEYS — это специальный "флаг",
        // который просит базу данных ВЕРНУТЬ значение автоматически
        // сгенерированного ключа (то есть id новой строки, присвоенный BIGSERIAL)
        // после выполнения INSERT. Обратите внимание: в ЭТОМ конкретном методе
        // сгенерированный ключ запрошен (RETURN_GENERATED_KEYS), но нигде дальше
        // в теле метода фактически НЕ считывается (обычно для этого после
        // executeUpdate() дополнительно вызывают ps.getGeneratedKeys() и читают
        // оттуда id) — то есть в текущей реализации этот флаг передан,
        // но эффекта на поведение метода не оказывает, так как результат
        // генерации ключа просто нигде не используется дальше по коду.
        try (Connection conn = DatabaseConnection.getConnection();
                PreparedStatement ps = conn.prepareStatement(sql, Statement.RETURN_GENERATED_KEYS)) {

            // ps.setString(1, ...) — подставляет строковое значение вместо ПЕРВОГО
            // по счёту "?" в SQL-запросе. Нумерация параметров PreparedStatement,
            // как и нумерация столбцов ResultSet, начинается с 1, а НЕ с 0.
            ps.setString(1, service.title());
            // ps.setInt(2, ...) — подставляет целое число вместо ВТОРОГО "?".
            ps.setInt(2, service.durationMinutes());
            // ps.setBigDecimal(3, ...) — есть отдельный метод именно для BigDecimal,
            // который корректно передаёт значение денежного типа в базу данных,
            // сохраняя точность (не через обычный setDouble/setString).
            ps.setBigDecimal(3, service.price());
            // ps.setString(4, ...) — подставляет описание вместо ЧЕТВЁРТОГО "?".
            ps.setString(4, service.description());

            // executeUpdate() — выполняет SQL-команду, которая изменяет данные
            // (INSERT/UPDATE/DELETE), и возвращает количество затронутых строк.
            // Здесь возвращаемое число НЕ сохраняется в переменную и не проверяется —
            // метод просто "выполняет и забывает" результат, полагаясь, что
            // если исключения не произошло, значит вставка прошла успешно.
            ps.executeUpdate();

        } catch (SQLException e) {
            // Если во время работы с базой данных произошла ошибка (SQLException,
            // checked-исключение), она здесь ПЕРЕХВАТЫВАЕТСЯ и "оборачивается"
            // в RuntimeException (unchecked-исключение). Смысл этого приёма:
            // интерфейс GroomingServiceRepository, судя по всему, объявляет метод
            // save() БЕЗ "throws SQLException" в сигнатуре (иначе можно было бы
            // просто пробросить исключение дальше через throws, как это сделано
            // в DatabaseConnection). Так как интерфейс не предполагает checked-исключений,
            // а бросить их из реализации метода интерфейса, если их нет в сигнатуре
            // самого интерфейса, НЕЛЬЗЯ (правило Java для переопределения методов),
            // единственный выход — "завернуть" checked SQLException в unchecked
            // RuntimeException, который можно бросать из любого метода без
            // объявления throws.
            // "new RuntimeException(сообщение, e)" — второй аргумент "e" (исходное
            // исключение) передаётся как ПРИЧИНА (cause) нового исключения — это
            // сохраняет исходную информацию об ошибке (в том числе stack trace
            // самого SQLException) внутри нового RuntimeException, что критически
            // важно для отладки: не потерять "откуда на самом деле пошла ошибка",
            // даже упаковывая её в другой тип исключения.
            throw new RuntimeException("Ошибка при сохранении услуги в БД: " + e.getMessage(), e);
        }
    }

    // Комментарий-описание метода.
    // Ищет одну услугу по уникальному ID.

    // Возвращаемый тип "Optional<GroomingService>" — метод ЯВНО сообщает
    // в своей сигнатуре: "результат может отсутствовать, и это нормально" —
    // вызывающий код обязан будет либо проверить Optional через
    // isPresent()/isEmpty(),
    // либо использовать методы вроде orElse()/orElseThrow(), и вряд ли случайно
    // забудет обработать случай "услуга не найдена", как это могло бы случиться
    // при обычном возврате null.
    @Override
    public Optional<GroomingService> findById(Long id) {
        // SQL-запрос на чтение (SELECT) с условием WHERE id = ? — параметр "?"
        // будет заменён на конкретное значение id ниже.
        String sql = "SELECT id, title, duration_minutes, price, description FROM grooming_services WHERE id = ?";

        try (Connection conn = DatabaseConnection.getConnection();
                PreparedStatement ps = conn.prepareStatement(sql)) {

            // ps.setLong(1, id) — подставляет значение id (тип Long) вместо
            // единственного "?" в запросе. Обратите внимание: метод setLong
            // принимает примитивный "long", поэтому если id — объектный Long,
            // здесь происходит АВТОМАТИЧЕСКАЯ РАСПАКОВКА (unboxing) Long -> long.
            // Если бы id оказался null, это привело бы к NullPointerException
            // именно на этой строке (распаковка null в примитив невозможна) —
            // в текущем коде проверки id на null нет, вызывающий код должен
            // сам гарантировать, что id не null.
            ps.setLong(1, id);

            // Вложенный try-with-resources для ResultSet — открывается только
            // на время чтения результата запроса и закрывается сразу после,
            // не дожидаясь конца всего метода findById.
            try (ResultSet rs = ps.executeQuery()) {
                // rs.next() — пытается перейти к первой строке результата.
                // Если строка есть (услуга с таким id найдена) — вернёт true.
                if (rs.next()) {
                    // Optional.of(...) — создаёт Optional, СОДЕРЖАЩИЙ значение
                    // (Optional.of() бросает исключение, если передать в него null,
                    // но здесь значение точно не null, так как оно только что
                    // построено из реально найденной строки результата).
                    // mapRowToGroomingService(rs) — вызов приватного вспомогательного
                    // метода этого же класса (объявлен в самом низу файла),
                    // который превращает "сырую" строку ResultSet в готовый
                    // объект GroomingService.
                    // "return" здесь завершает выполнение ВСЕГО метода findById
                    // сразу, как только строка найдена — код ниже (после
                    // закрывающих скобок try) в этом случае не выполнится.
                    return Optional.of(mapRowToGroomingService(rs));
                }
                // Если rs.next() вернул false (результат пуст, услуга не найдена),
                // мы просто "проваливаемся" из блока if без return — управление
                // дойдёт до конца метода, где стоит "return Optional.empty();".
            }

        } catch (SQLException e) {
            // Аналогично save() — оборачиваем checked SQLException в unchecked
            // RuntimeException, чтобы не менять сигнатуру метода интерфейса.
            // В сообщение исключения дополнительно подставлен сам id,
            // по которому шёл поиск — это удобно при отладке: сразу видно,
            // ДЛЯ КАКОГО именно id запрос завершился ошибкой.
            throw new RuntimeException("Ошибка при поиске услуги по ID=" + id + ": " + e.getMessage(), e);
        }

        // Эта строка выполнится в ДВУХ случаях:
        // 1) rs.next() вернул false внутри try (услуга с таким id не найдена) —
        // тогда мы "дошли" сюда естественным путём, минуя return внутри if;
        // 2) теоретически — если бы try завершился без исключения и без явного
        // return изнутри (в текущей логике это фактически совпадает со случаем 1).
        // Optional.empty() — специальный "пустой" Optional, явно показывающий
        // вызывающему коду: "значения нет, это нормальный, ожидаемый результат".
        return Optional.empty();
    }

    // Комментарий-описание метода.
    // Возвращает все услуги из таблицы в порядке возрастания ID.
    @Override
    public List<GroomingService> findAll() {
        // Список, в который будем СОБИРАТЬ результаты построчно — заранее
        // создаём его ПУСТЫМ, а затем наполняем в цикле ниже.
        // Объявлен как "List<GroomingService>" (тип интерфейса), а создаётся
        // как "new ArrayList<>()" (конкретная реализация) — стандартный
        // приём "программируй на уровне интерфейса".
        List<GroomingService> services = new ArrayList<>();

        // ORDER BY id — явно указываем базе данных, в каком порядке вернуть
        // строки (по возрастанию id), чтобы порядок результата был предсказуемым
        // и не зависел от внутреннего, никак не гарантированного порядка хранения
        // строк на диске.
        String sql = "SELECT id, title, duration_minutes, price, description FROM grooming_services ORDER BY id";

        // Здесь СРАЗУ ТРИ ресурса в try-with-resources (через точку с запятой):
        // Connection, PreparedStatement и ResultSet — в отличие от findById,
        // где ResultSet был во ВЛОЖЕННОМ try. Так как в findAll() ResultSet
        // нужен НА ВЕСЬ метод целиком (пока не переберём все строки в цикле while),
        // а не только на короткий фрагмент кода, его закрытие можно спокойно
        // указать в том же самом try, что и Connection/PreparedStatement —
        // это просто вопрос удобства и области видимости переменной.
        try (Connection conn = DatabaseConnection.getConnection();
                PreparedStatement ps = conn.prepareStatement(sql);
                ResultSet rs = ps.executeQuery()) {

            // while (rs.next()) — классический паттерн обхода ВСЕХ строк результата
            // (в отличие от findById, где было одиночное "if (rs.next())" —
            // ведь там ожидалась максимум ОДНА строка по уникальному id,
            // а здесь строк может быть сколько угодно).
            while (rs.next()) {
                // На каждой итерации превращаем текущую строку курсора в объект
                // GroomingService и добавляем в список результатов.
                services.add(mapRowToGroomingService(rs));
            }

        } catch (SQLException e) {
            throw new RuntimeException("Ошибка при получении списка услуг: " + e.getMessage(), e);
        }

        // Возвращаем накопленный список. Если услуг в базе не было вообще,
        // сюда попадёт просто пустой список (не null) — вызывающему коду
        // не нужно отдельно проверять на null, достаточно проверить
        // services.isEmpty(), если это важно.
        return services;
    }

    // Комментарий-описание метода.
    // Обновляет данные услуги в БД.

    // Возвращаемый тип "boolean" (а не void, как в save()) — метод явно
    // сообщает вызывающему коду, УДАЛОСЬ ЛИ обновление на самом деле
    // (например, если услуги с таким id уже не существует, update ничего
    // не изменит, и это важно уметь отличить от "обновление прошло успешно").
    @Override
    public boolean update(GroomingService service) {
        // SET title = ?, ... WHERE id = ? — обновляем ВСЕ поля, кроме id,
        // а сам id используется только в условии WHERE, чтобы определить,
        // КАКУЮ именно строку менять.
        String sql = "UPDATE grooming_services SET title = ?, duration_minutes = ?, price = ?, description = ? WHERE id = ?";

        try (Connection conn = DatabaseConnection.getConnection();
                PreparedStatement ps = conn.prepareStatement(sql)) {

            // Первые 4 параметра — новые значения полей (в том же порядке,
            // что и в SET title = ?, duration_minutes = ?, price = ?, description = ?).
            ps.setString(1, service.title());
            ps.setInt(2, service.durationMinutes());
            ps.setBigDecimal(3, service.price());
            ps.setString(4, service.description());
            // Пятый параметр — id услуги, которую нужно найти для обновления
            // (соответствует ПЯТОМУ "?" в тексте SQL — тому, что после WHERE id = ?).
            // Здесь service.id() возвращает Long, а setLong ожидает примитивный long,
            // поэтому опять происходит автоматическая распаковка (unboxing) —
            // если бы у переданного объекта service поле id было null
            // (например, для ещё не сохранённой в базу услуги), здесь возникла бы
            // NullPointerException.
            ps.setLong(5, service.id());

            // executeUpdate() для UPDATE возвращает количество ФАКТИЧЕСКИ
            // изменённых строк в базе данных (0, если строка с таким id
            // не найдена, и запрос никого не обновил; 1 — если такая строка есть
            // и была обновлена; больше 1 обычно не ожидается, так как id уникален).
            int updatedRows = ps.executeUpdate();
            // "updatedRows > 0" — превращаем число изменённых строк в простой
            // булев ответ: "хотя бы что-то реально обновилось" (true) или
            // "ничего не изменилось, скорее всего, услуги с таким id не было" (false).
            return updatedRows > 0;

        } catch (SQLException e) {
            throw new RuntimeException("Ошибка при обновлении услуги с ID=" + service.id() + ": " + e.getMessage(), e);
        }
    }

    // Комментарий-описание метода.
    // Удаляет услугу из таблицы по ID.

    // Аналогично update() — возвращает boolean, чтобы явно показать,
    // действительно ли что-то было удалено, а не просто "запрос выполнился без
    // ошибок".
    @Override
    public boolean deleteById(Long id) {
        String sql = "DELETE FROM grooming_services WHERE id = ?";

        try (Connection conn = DatabaseConnection.getConnection();
                PreparedStatement ps = conn.prepareStatement(sql)) {

            ps.setLong(1, id);

            // Как и в update(), executeUpdate() для DELETE возвращает количество
            // фактически удалённых строк — 0, если строки с таким id не было,
            // и обычно 1, если строка была найдена и удалена (так как id — первичный
            // ключ, дублей быть не может).
            //
            // Важная деталь предметной области, которую стоит держать в голове:
            // в таблице appointments внешний ключ service_id объявлен с
            // "ON DELETE CASCADE" (см. комментарии к DatabaseConnection) —
            // а значит, удаление услуги здесь АВТОМАТИЧЕСКИ приведёт к удалению
            // ВСЕХ записей на приём, ссылавшихся на эту услугу, прямо на уровне
            // базы данных, без какого-либо дополнительного кода в этом методе.
            int deletedRows = ps.executeUpdate();
            return deletedRows > 0;

        } catch (SQLException e) {
            throw new RuntimeException("Ошибка при удалении услуги с ID=" + id + ": " + e.getMessage(), e);
        }
    }

    // Комментарий-описание метода.
    // Преобразует строку из результата SQL-запроса в объект GroomingService.

    // "private" — метод является ЧИСТО ВНУТРЕННЕЙ технической деталью реализации,
    // используется только внутри этого класса (в findById и findAll) и не должен
    // быть виден или вызываться откуда-либо ещё — он не часть "контракта"
    // репозитория (интерфейс GroomingServiceRepository про него ничего не знает).
    // "throws SQLException" — этот метод НЕ ловит исключение сам, а перекладывает
    // его на вызывающий код (то есть на findById/findAll, где он вызывается
    // ВНУТРИ их собственных try-catch блоков, которые как раз и ловят SQLException
    // и оборачивают её в RuntimeException).
    private GroomingService mapRowToGroomingService(ResultSet rs) throws SQLException {
        // Создаём новый объект record GroomingService (см. отдельно
        // прокомментированный файл модели), считывая значения из ТЕКУЩЕЙ строки
        // курсора ResultSet — по ИМЕНАМ столбцов (а не по номерам, как в setXxx
        // у PreparedStatement выше). Чтение по имени столбца (rs.getLong("id")
        // вместо rs.getLong(1)) делает код более УСТОЙЧИВЫМ к изменению порядка
        // столбцов в SQL-запросе SELECT — даже если кто-то поменяет местами
        // колонки в запросе, код продолжит работать правильно, так как ищет
        // значение по названию столбца, а не по позиции.
        return new GroomingService(
                rs.getLong("id"), // Long id — читаем колонку "id" как long.
                rs.getString("title"), // String title — колонка "title" как строка.
                rs.getInt("duration_minutes"), // int durationMinutes — колонка "duration_minutes" как число.
                rs.getBigDecimal("price"), // BigDecimal price — специальный метод для денежного типа,
                                           // сохраняющий точность значения из колонки DECIMAL(10,2).
                rs.getString("description")); // String description — колонка "description" как строка.
    }
}